import React, { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom';

// View holdings
function UserStockHoldings() {
    
    const navigate = useNavigate();

    return (
        <div>UserStockHoldings</div>
    )
}

export default UserStockHoldings